name: Testes Integrados de API com Newman

on:
  pull_request:
    branches:
    - master
  push:
    branches:
    - master
    - test

jobs:

  newman-test:
  
    uses: Vis-Rodrigues/workflow-template/.github/workflows/test-api-newman.yml@main
      inputs:
        collection: postman_collection
        environment: postman_environment_prd